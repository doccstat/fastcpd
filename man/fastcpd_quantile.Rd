% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fastcpd.R
\name{fastcpd_quantile}
\alias{fastcpd_quantile}
\alias{fastcpd.quantile}
\title{Find change points efficiently in quantile regression models}
\usage{
fastcpd_quantile(data, order = 0.5, ...)

fastcpd.quantile(data, order = 0.5, ...)
}
\arguments{
\item{data}{A matrix or a data frame with the response variable as the first
column and covariates in the remaining columns.}

\item{order}{Quantile level \eqn{\tau}, a numeric value in (0, 1). The
default is 0.5, corresponding to median regression.}

\item{...}{Other arguments passed to \code{\link[=fastcpd]{fastcpd()}}, for example,
\code{segment_count}.}
}
\value{
A \link[=fastcpd-class]{fastcpd} object.
}
\description{
\code{\link[=fastcpd_quantile]{fastcpd_quantile()}} and \code{\link[=fastcpd.quantile]{fastcpd.quantile()}} are wrapper
functions of \code{\link[=fastcpd]{fastcpd()}} to detect change points in quantile regression
models using the pinball (check function) loss
\eqn{\rho_\tau(u) = u(\tau - \mathbf{1}_{u < 0})}.
The function detects changes in the conditional \eqn{\tau}-quantile of
the response given the covariates. The segment cost is minimised via
iteratively reweighted least squares (IRLS).
}
\examples{
set.seed(1)
n <- 200
p <- 2
x <- matrix(rnorm(n * p), n, p)
theta_1 <- c(1, -1)
theta_2 <- c(-1, 1)
y <- c(
  x[1:100, ] \%*\% theta_1 + rnorm(100),
  x[101:200, ] \%*\% theta_2 + rnorm(100)
)
result <- fastcpd_quantile(cbind(y, x), order = 0.5)
summary(result)
set.seed(42)
n <- 400
x <- rep(1, n)
eps <- rnorm(n)
eps[c(50, 51, 52, 300, 301, 302)] <- -15
y <- c(0 + eps[1:200], 3 + eps[201:400])
dat <- cbind(y, x)
# lm is dominated by the six -15 outliers and reports spurious breaks
result_lm <- fastcpd_lm(dat)
# Quantile regression (tau=0.5) is robust to outliers below 50\% and
# correctly finds only the true change at 200.
result_qr <- fastcpd_quantile(dat, order = 0.5)
}
\seealso{
\code{\link[=fastcpd]{fastcpd()}}
}
