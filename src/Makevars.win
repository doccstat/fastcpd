PKG_CXXFLAGS = $(SHLIB_OPENMP_CXXFLAGS) -I.
PKG_LIBS = $(SHLIB_OPENMP_CXXFLAGS) $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS)

# Pick up abseil flags generated by configure.win (silently ignored when absent).
-include Makevars.abseil
